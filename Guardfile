# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do

  watch(%r{\.(php)$})
  watch(%r{View/.+\.(ctp)$})
  watch(%r{webroot/.+\.(css|js)})
  watch(%r{Config/.+\.yml})

end
